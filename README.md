# Tunuk - Beauty Services Platform

Платформа для домашних услуг в Центральной Азии, предлагающая выездные бьюти-услуги (салон на дому, спа, вечерний макияж, массаж, стрижка, уход за ногтями) и другие бытовые сервисы.

## 🚀 Особенности

- **Высокий рейтинг мастеров**: Все профессионалы имеют рейтинг выше 4.8
- **Адаптивный дизайн**: Работает на всех устройствах
- **3D иконки**: Современный визуальный дизайн
- **Кабинеты пользователей**: Отдельные кабинеты для клиентов, мастеров и администраторов
- **Мок-данные**: Работает без backend для демонстрации

## 🛠 Технологии

### Backend
- FastAPI (Python)
- SQLAlchemy
- SQLite (по умолчанию) / PostgreSQL (опционально)
- JWT аутентификация
- Alembic для миграций

### Frontend
- React 18
- Vite
- React Router DOM
- Axios
- Lucide React (иконки)
- date-fns

## 📦 Установка

### Backend

```bash
cd backend
pip install -r requirements.txt
```

### Frontend

```bash
cd frontend
npm install
```

## 🏃 Запуск

### Backend

```bash
cd backend
python app/init_db.py  # Создать тестовые данные
uvicorn app.main:app --reload --port 8080
```

### Frontend

```bash
cd frontend
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5174

## 📝 Тестовые данные

### Администратор
- Email: `admin@example.com`
- Пароль: `admin123`

### Клиенты
- Email: `client1@example.com`
- Пароль: `password123`

### Мастера
- Email: `master1@example.com`
- Пароль: `password123`

## 🔧 Мок-данные

Платформа может работать с мок-данными без backend. Для включения/отключения:

```javascript
// В консоли браузера
localStorage.setItem("useMockData", "true")  // Включить
localStorage.setItem("useMockData", "false") // Отключить
```

Или используйте переключатель в интерфейсе (в режиме разработки).

## 📱 Структура проекта

```
beauty/
├── backend/          # FastAPI backend
│   ├── app/
│   │   ├── routers/  # API роутеры
│   │   ├── models.py # Модели БД
│   │   ├── schemas.py# Pydantic схемы
│   │   └── main.py   # Главный файл
│   └── requirements.txt
│
├── frontend/         # React frontend
│   ├── src/
│   │   ├── pages/    # Страницы
│   │   ├── components/ # Компоненты
│   │   ├── contexts/ # React контексты
│   │   ├── services/ # API сервисы
│   │   └── mocks/    # Мок-данные
│   └── package.json
│
└── README.md
```

## 🌐 Деплой на GitHub Pages

Проект настроен для автоматического деплоя на GitHub Pages через GitHub Actions.

### Настройка

1. Создайте репозиторий на GitHub
2. Запушьте код:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/yourusername/tunuk.git
   git push -u origin main
   ```

3. В настройках репозитория:
   - Settings → Pages
   - Source: GitHub Actions

4. GitHub Actions автоматически задеплоит проект при каждом push в main

## 📄 API Документация

После запуска backend, документация доступна по адресу:
- Swagger UI: http://localhost:8080/docs
- ReDoc: http://localhost:8080/redoc

## 🎨 Кабинеты пользователей

- **Кабинет клиента** (`/client`): Управление заказами, отзывы, избранные мастера
- **Кабинет мастера** (`/professional`): Управление услугами, бронирования, статистика
- **Админ-панель** (`/admin`): Управление системой, пользователями, услугами

## 📞 Контакты

Email: info@tunuk.kg

## 📜 Лицензия

MIT License
